package org.opencourse.utils.typeinfo;

/**
 * Types information interface for OpenCourse.
 * This interface provides methods to retrieve from course types, resource types and user history action types.
 * 
 * @author !EEExp3rt
 */
public interface TypeInfo {
    byte getId();
    String getName();
    String getDescription();
}
